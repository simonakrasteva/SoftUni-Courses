static int FindGreaterNumber(int num, int num2)
{
    if (num > num2)
    {
        return num;
    } else
    {
        return num2;
    }
}

static char FindGreaterChar(char ch, char ch2)
{
    if (ch > ch2)
    {
        return ch;
    } else
    {
        return ch2;
    }
}

static string FindGreaterString(string str, string str2)
{
     if (str.CompareTo(str2) > 0)
    {
        return str;
    } else
    {
        return str2;
    }
}

string type = Console.ReadLine();

if (type == "int")
{
    int num = int.Parse(Console.ReadLine());
    int num2 = int.Parse(Console.ReadLine());

    Console.WriteLine(FindGreaterNumber(num, num2));
} else if (type == "char")
{
    char ch = char.Parse(Console.ReadLine());
    char ch2 = char.Parse(Console.ReadLine());

    Console.WriteLine(FindGreaterChar(ch, ch2));
} else if (type == "string")
{
    string str = Console.ReadLine();
    string str2 = Console.ReadLine();

    Console.WriteLine(FindGreaterString(str, str2));
}
