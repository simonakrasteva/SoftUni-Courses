int n = int.Parse(Console.ReadLine());
int result = 0;

for (int firstNum = 2; firstNum <= n; firstNum += 2)
{
    for (int secondNum = 1; secondNum <= n; secondNum += 2)
    {
        result = firstNum * secondNum;

        Console.Write($"{firstNum}{secondNum}{result} ");
    }
    
}
