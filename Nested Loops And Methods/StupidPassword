int n = int.Parse(Console.ReadLine());
int result = 0;

for (int i = 2; i <= n; i += 2)
{
    for (int j = 1; j <= n; j += 2)
    {
        result = i * j;

        Console.WriteLine($"{i}{j}{result}");
    }
    
}
